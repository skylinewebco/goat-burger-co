# build tooling / local
node_modules/
# personal / scratch (do not publish)
chat-extract.txt
*.tmp
# dev-only render artifacts
debug-*.png

# large source assets (inputs to process-assets.js / draw-box.js)
asset-*.png
